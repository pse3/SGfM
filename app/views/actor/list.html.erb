<h3>Actors of User</h3>

<% for actor in @actors %>
  <p>
    <a href="<%= show_actor_path(actor) %>"><%=  actor.name %> (<%= actor.actor_type.name %>) created: <%= actor.created_at %></a>
  </p>
<% end %>