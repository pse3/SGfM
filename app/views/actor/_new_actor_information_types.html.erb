Loaded this per ajay. Yay! :) Your key is: <%= actor_type_key %>. Use it wisely!:)

The object itself <%= actor_type %>

<% information_types.each do |info_type| %>
  tada <%=  info_type %>
<% end %>
